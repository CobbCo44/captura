// Phase 5 — Doctor sets/updates patient protocol
exports.handler = async (event) => {
  return {
    statusCode: 200,
    body: JSON.stringify({ message: 'update-protocol: coming in Phase 5' })
  };
};
